{
  "name": "thinking-break-codex",
  "displayName": "Thinking Break for Codex",
  "description": "Play a fast browser FPS while OpenAI Codex CLI is working \u2014 it resumes when the agent goes busy and pauses when it finishes.",
  "version": "1.0.0",
  "publisher": "doringber",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Doringber/thinkingbreak.git"
  },
  "homepage": "https://doringber.github.io/thinkingbreak/",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "agent",
    "game",
    "fps",
    "claude",
    "cursor",
    "codex",
    "break"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "thinking-break-codex.openGame",
        "title": "Thinking Break: Open Game"
      },
      {
        "command": "thinking-break-codex.closeGame",
        "title": "Thinking Break: Close Game Panel"
      },
      {
        "command": "thinking-break-codex.installHooks",
        "title": "Thinking Break: Install Codex CLI Hooks"
      }
    ],
    "configuration": {
      "title": "Thinking Break for Codex",
      "properties": {
        "thinkingBreakCodex.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable Thinking Break."
        },
        "thinkingBreakCodex.gameUrl": {
          "type": "string",
          "default": "https://doringber.github.io/thinkingbreak/fps/",
          "description": "URL of the Thinking Break game to open."
        },
        "thinkingBreakCodex.delaySeconds": {
          "type": "number",
          "default": 3,
          "minimum": 0,
          "description": "Seconds to wait after Codex starts working before opening or resuming the game."
        },
        "thinkingBreakCodex.idleGraceSeconds": {
          "type": "number",
          "default": 1.2,
          "minimum": 0,
          "description": "Ignore an idle signal that arrives this soon after a busy one, so the game does not pause between consecutive tool calls."
        },
        "thinkingBreakCodex.onIdle": {
          "type": "string",
          "enum": [
            "pauseAndFocusEditor",
            "pauseOnly",
            "pauseAndClose"
          ],
          "default": "pauseAndFocusEditor",
          "enumDescriptions": [
            "Pause the game, save the session, and reopen whichever side bars were closed to make room for it \u2014 so you land back on the conversation, not a blank editor. (Panel stays open so the next task resumes instantly.)",
            "Pause the game and leave focus where it is.",
            "Pause the game and close the panel. The next task reopens it from the saved session."
          ],
          "description": "What to do when Codex finishes a task."
        },
        "thinkingBreakCodex.hidePanelsWhenPlaying": {
          "type": "boolean",
          "default": true,
          "description": "Collapse sidebars when opening the game so the viewport is landscape."
        },
        "thinkingBreakCodex.openInBackground": {
          "type": "boolean",
          "default": false,
          "description": "Open the game panel without stealing focus from the editor."
        },
        "thinkingBreakCodex.openInExternalBrowser": {
          "type": "boolean",
          "default": false,
          "description": "Open the game in your system browser instead of inside VS Code."
        },
        "thinkingBreakCodex.useSimpleBrowser": {
          "type": "boolean",
          "default": false,
          "description": "Use the built-in Simple Browser instead of the Thinking Break webview panel. Pause and resume then require a page reload."
        },
        "thinkingBreakCodex.processPolling": {
          "type": "boolean",
          "default": true,
          "description": "Also detect a running `codex` process with pgrep, as a fallback when hooks are unavailable."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package --no-dependencies --allow-missing-repository"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.6.0"
  }
}
